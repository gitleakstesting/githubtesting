name: Secret Scan

on:
  push:
  pull_request:

jobs:
  call-secret-scan:
    uses: gitleakstesting/security-policies/.github/workflows/gitleaks.yml@main
    with:
      config_path: ".github/gitleaks.toml"
